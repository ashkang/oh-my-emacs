;; Custom Bindings
(defconst bd-comment-uncomment "C-`")
(defconst bd-godefjump "C-=")
(defconst bd-indent-according-to-mode "C-'")
(defconst bd-replace-string "C-#")
(defconst bd-replace-regexp "C-$")
(defconst bd-replace-last-sexp "C-%")
(defconst bd-compile "C-^")

;; Autocomplete
(defconst bd-autocomplete "C-TAB")

;; CEDET
(defconst bd-semantic-ia-complete-symbol-menu "M-<RET>")

;; IDO
(defconst bd-ido-next-match "C-n")
(defconst bd-ido-prev-match "C-p")

;; Evil Numbers
(defconst bd-evil-numbers-inc-at-pt "C-c +")
(defconst bd-evil-numbers-dec-at-pt "C-c -")

;; Multiple Cursors
(defconst bd-mc-edit-lines "C-.")
(defconst bd-mc-mark-next-like-this "C->")
(defconst bd-mc-mark-previous-like-this "C-<")
(defconst bd-mc-mark-all-like-this "C-c C-<")

;; Projectile
(defconst bd-projectile-find-file "s-p")
(defconst bd-projectile-switch-to-buffer "s-b")

;; Transparency
(defconst bd-transparency-set-value "C-?")
(defconst bd-transparency-increase "C-)")
(defconst bd-transparency-decrease "C-(")
